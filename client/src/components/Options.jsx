export const options = (props) => {
    return (
        [
            { key: 1, text: props.brackets[props.rowNum].a.name, value: props.brackets[props.rowNum].a.name, seed: props.brackets[props.rowNum].a.seed },
            { key: 2, text: props.brackets[props.rowNum].b.name, value: props.brackets[props.rowNum].b.name, seed: props.brackets[props.rowNum].b.seed },
        ]
    )
}

export const options2 = (props) => {
    return (
        [
            { key: 1, text: props.brackets[props.rowNum].a.name, value: props.brackets[props.rowNum].a.name, seed: props.brackets[props.rowNum].a.seed },
            { key: 2, text: props.brackets2[props.rowNum].b.name, value: props.brackets2[props.rowNum].b.name, seed: props.brackets2[props.rowNum].b.seed },
        ]
    )
}

export const options3 = (props) => {
    return (
        [
            { key: 1, text: props.brackets[props.rowNum].a.name, value: props.brackets[props.rowNum].a.name, seed: props.brackets[props.rowNum].a.seed },
            { key: 2, text: props.brackets[props.rowNum2].a.name, value: props.brackets[props.rowNum2].a.name, seed: props.brackets[props.rowNum2].a.seed },
        ]
    )
}
export const options4 = (props) => {
    return (
        [
            { key: 1, text: props.brackets[props.rowNum].a.name, value: props.brackets[props.rowNum].a.name, seed: props.brackets[props.rowNum].a.seed },
            { key: 2, text: props.brackets2[props.rowNum2].a.name, value: props.brackets2[props.rowNum2].a.name, seed: props.brackets2[props.rowNum2].a.seed },
        ]
    )
}




